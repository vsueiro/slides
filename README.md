# slides

Presentation slides from markdown files, using a built-in static site generator (Jekyll).

To run locally, install Jekyll, open Terminal, `cd` to root of the repository and run `jekyll s -l -o` to serve the website.